// JavaScript source code
function fightGenerator() {
    const MAX = 10;
    var num = Math.trunc(MAX * Math.random()) + 1;
    incontro.src = "img/0" + num + ".jpg";
    nome.innerHTML = "hai estratto la carta " + num;
}